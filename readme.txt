This graduation.py file contains the function which would take input n ie the total days to the graduation and m represents the consecutive classes that a student cannot miss ie by default is 4.

To run the  script use Python 3

commands: Python garduation.py
input n = It should be an integer value representing total number of days for graduation

result is in the format:
"(The probability that you will miss your graduation ceremony) / (The number of ways to attend classes over N days)"
   
